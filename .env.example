# Supabase (same URL used by Next.js client and Fastify API)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_API_URL=http://localhost:4000

# API-only: service role key (never expose to the client)
SUPABASE_SERVICE_ROLE_KEY=
API_PORT=4000
